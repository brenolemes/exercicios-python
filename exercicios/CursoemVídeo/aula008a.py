# UTILIZANDO MÓDULOS
# Da pra importar o módulo geral (import math) e algo especifico do módulo (from math import sqrt)

# from math import sqrt, floor
# num = int(input('Dígite um número: '))
# raiz = sqrt(num)
# print('A raiz quadrada de {} é {}'.format(num, floor(raiz)))

# import random
# num = random.randint(1, 10)
# print(num)

# É possível também importar além do que vem na biblioteca padrão do python (python.org/pypi)

import emoji
print(emoji.emojize("Olá, Mundo :earth_americas:", use_aliases=True))
